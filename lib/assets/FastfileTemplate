before_all do
  # system("./customShellScript.sh")
  install_cocoapods
end

lane :test do 
  xctool test
  snapshot
end

lane :beta do
  xctool test
  snapshot
  # sigh
  # system('your_script.sh')
end

lane :deploy do
  xctool test
  snapshot
  # sigh
  deliver
end

after_all do
  # Send message on Slack, send success Email, ...
end