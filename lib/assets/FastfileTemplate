before_all do
  # sh "./customShellScript.sh"
  install_cocoapods
end

lane :test do 
  xctool "test"
  snapshot
end

lane :beta do
  xctool "test"
  snapshot
  # sigh
  deliver :skip_deploy, :beta
  # sh "your_script.sh"
end

lane :deploy do
  xctool "test"
  snapshot
  # sigh
  deliver :skip_deploy, :force
  # frameit
end

after_all do
  # Send message on Slack, send success Email, ...
end