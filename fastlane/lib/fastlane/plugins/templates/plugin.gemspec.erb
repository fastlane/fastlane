# coding: utf-8
lib = File.expand_path("../lib", __FILE__)
$LOAD_PATH.unshift(lib) unless $LOAD_PATH.include?(lib)
require '<%= require_path %>/version'

Gem::Specification.new do |spec|
  spec.name          = '<%= gem_name %>'
  spec.version       = Fastlane::<%= plugin_name.capitalize %>::VERSION
  spec.author        = %q{<%= author %>}
  spec.email         = %q{<%= email %>}

  spec.summary       = %q{<%= plugin_name %>: A fastlane plugin}
  spec.description   = %q{<%= plugin_name %> is a fastlane plugin}
  spec.homepage      = "https://github.com/<GITHUB_USERNAME>/<%= gem_name %>"
  spec.license       = "MIT"

  spec.files         = Dir["lib/**/*"] + %w(README.md LICENSE)
  spec.test_files    = spec.files.grep(%r{^(test|spec|features)/})
  spec.require_paths = ['lib']

  # spec.add_dependency 'your-dependency', '~> 1.0.0'
end
