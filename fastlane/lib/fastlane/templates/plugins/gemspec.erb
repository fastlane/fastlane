# coding: utf-8
lib = File.expand_path('../lib', __FILE__)
$LOAD_PATH.unshift(lib) unless $LOAD_PATH.include?(lib)
require '<%= gem_name %>/version'

Gem::Specification.new do |spec|
  spec.name          = "<%= gem_name %>"
  spec.version       = <%= gem_module %>::VERSION
  spec.authors       = <%= gem_authors %>
  #spec.email         = ["fastlane-xcversion@gmail.com"]

  #spec.summary       = %q{Something random}
  #spec.homepage      = "https://github.com/fastlane/fastlane-xcversion"
  #spec.license       = "MIT"

  #spec.files         = `git ls-files -z`.split("\x0").reject { |f| f.match(%r{^(test|spec|features)/}) }
  #spec.require_paths = ["lib"]

  #spec.add_dependency 'xcode-install', '~> 1.3.0'
end
