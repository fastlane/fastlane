machine:
  environment:
    FL_BUILDLOG_PATH: ~/Library/Logs
  xcode:
    version: "7.3"
dependencies:
  pre:
    - brew install shellcheck
  override:
    - bundle install --jobs=4 --retry=3
test:
  pre:
    - ulimit -n 400
  override:
    - bundle exec danger # for quick, instant feedback of PR body and title
    - bundle exec fastlane test
    - bundle exec danger # for posting the test results to GitHub
