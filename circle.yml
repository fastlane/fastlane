machine:
  xcode:
    version: "7.3"
dependencies:
  override:
    - bundle install --jobs=4 --retry=3
test:
  override:
    - bundle exec danger
    - bundle exec rake test_all
    - bundle exec danger
