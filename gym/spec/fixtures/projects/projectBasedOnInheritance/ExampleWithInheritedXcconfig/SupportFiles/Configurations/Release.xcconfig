#include "../Common/Variables.xcconfig"

// Packaging

PRODUCT_BUNDLE_IDENTIFIER = $(_RELEASE_APP_PRODUCT_BUNDLE_IDENTIFIER)

// Signing

CODE_SIGN_IDENTITY = $(_RELEASE_CODE_SIGN_IDENTITY)
DEVELOPMENT_TEAM = $(_RELEASE_DEVELOPMENT_TEAM)
PROVISIONING_PROFILE_SPECIFIER = $(_RELEASE_PROVISIONING_PROFILE_SPECIFIER)
